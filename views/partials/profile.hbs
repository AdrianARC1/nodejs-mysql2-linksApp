<div class="container p-4">
    <div class="row">
        <div class="col-md-4">
            <div class="card text-center">
                <div class="card-body">
                    <h3>Bienvenido {{user.fullname}}</h3>
                    <h4>{{user.username}}</h4>
                    <a href="/links"><button class="btn btn-primary m-4">Haz tus links</button></a>
                </div>
            </div>
        </div>
    </div>
</div>